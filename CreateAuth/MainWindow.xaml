<Window x:Class="CreateAuth.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreateAuth"
        mc:Ignorable="d"
        Title="Авторизация" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Логин"/>
        <TextBlock Text="Пароль" Grid.Row="1"/>
        <TextBox Name="login" Grid.Column="1" Grid.Row="0"/>
        <PasswordBox Name="password" Grid.Column="1" Grid.Row="1"/>
        <Button Grid.Row="2" Grid.Column="0" Content="Войти" Click="Button_Click"></Button>
        <Button Grid.Row="2" Grid.Column="1" Content="Зарегистрироваться" Click="Button_Click_1"></Button>

    </Grid>
</Window>
